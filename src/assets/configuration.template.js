// Author: Preston Lee

(function(window) {
    // Environment variables
    window["PATIENT_PORTAL_DEFAULT_FHIR_URL"] = "${PATIENT_PORTAL_DEFAULT_FHIR_URL}";
    window["PATIENT_PORTAL_CONSENT_CDS_ROOT_URL"] = "${PATIENT_PORTAL_CONSENT_CDS_ROOT_URL}";
})(this);